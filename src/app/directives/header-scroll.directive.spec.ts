import { HeaderScrollDirective } from './header-scroll.directive';

describe('HeaderScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new HeaderScrollDirective();
    expect(directive).toBeTruthy();
  });
});
